- clean: ['~']

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.curlrc: .curlrc
    ~/.nanorc: .nanorc
    ~/.gitignore: .gitignore
    ~/.config/fish/config.fish: config.fish 
    ~/.face: assets/.face

- create:
    - ~/downloads
